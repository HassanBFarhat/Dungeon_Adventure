package models;

/**
 *  Enums describing the various items a room is able to hold.
 *
 *  @author Avinash Bavisetty
 *  @author Hassan Bassam Farhat
 *  @version Summer 2023
 */
public enum RoomItems {

    /** The entrance enum. */
    ENTRANCE,
    /** The exit enum. */
    EXIT,
    /** The healing potion enum. */
    HEALING_POTION,
    /** The pit enum. */
    PIT,
    /** The Abstraction pillar enum. */
    ABSTRACTION_PILLAR,
    /** The Encapsulation pillar enum. */
    ENCAPSULATION_PILLAR,
    /** The Inheritance pillar enum. */
    INHERITANCE_PILLAR,
    /** The Polymorphism pillar enum. */
    POLYMORPHISM_PILLAR

}
